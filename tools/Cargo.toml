[package]
name = "tools"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1.89"
serde_json.workspace = true
agent-core = { path = "../core" }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
tempfile = "3.8"
